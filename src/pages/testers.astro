---
import {
  ArrowLeft,
  UserPlus,
  Download,
  MessageSquare,
  ShieldCheck,
  ChevronRight
} from "lucide-astro";
import "../styles/global.css";
import SEO from "../components/SEO.astro";

const steps = [
  {
    id: "01",
    title: "Únete al Programa",
    description: "Google Play requiere que aceptes convertirte en tester oficial para habilitar la descarga de la beta.",
    link: "https://play.google.com/apps/testing/org.christophertwo.parse",
    buttonText: "Convertirme en Tester",
    icon: UserPlus,
    color: "bg-purple-100 text-purple-600",
    details: [
      "Accede con tu cuenta de Google principal",
      "Haz clic en 'Become a Tester'",
      "Espera unos minutos para la sincronización"
    ]
  },
  {
    id: "02",
    title: "Descarga la App",
    description: "Una vez aceptado, la Play Store te permitirá instalar la versión Beta de Parse directamente en tu dispositivo.",
    link: "https://play.google.com/store/apps/details?id=org.christophertwo.parse",
    buttonText: "Instalar desde Play Store",
    icon: Download,
    color: "bg-blue-100 text-blue-600",
    details: [
      "Busca 'Parse' en la Google Play Store",
      "Verás la etiqueta (Beta) junto al nombre",
      "Instala o actualiza la aplicación"
    ]
  },
  {
    id: "03",
    title: "Comparte tu Feedback",
    description: "¿Encontraste un bug o tienes una idea? Tu opinión es lo que hace que Parse siga mejorando.",
    link: "https://instagram.com/overridellc",
    buttonText: "Enviar Sugerencia",
    icon: MessageSquare,
    color: "bg-zinc-100 text-zinc-900",
    details: [
      "Reporta fallos en el renderizado",
      "Sugiere nuevas funcionalidades",
      "Etiquétanos en Instagram @overridellc"
    ]
  }
];
---

<!doctype html>
<html lang="es" class="scroll-smooth">
  <head>
    <SEO
      title="Programa de Testers - Parse"
      description="Sigue estos pasos para convertirte en tester de Parse y probar las últimas funciones antes que nadie."
    />
  </head>
  <body class="bg-white text-zinc-600 font-['Poppins',sans-serif] antialiased min-h-screen">
    <!-- Background Grid -->
    <div class="fixed inset-0 z-0 pointer-events-none">
      <div class="absolute inset-0 bg-[linear-gradient(to_right,#00000008_1px,transparent_1px),linear-gradient(to_bottom,#00000008_1px,transparent_1px)] bg-[size:24px_24px]"></div>
    </div>

    <!-- Nav -->
    <nav class="fixed top-0 w-full z-50 border-b border-zinc-200 bg-white/70 backdrop-blur-xl">
      <div class="max-w-5xl mx-auto px-6 h-16 flex items-center justify-between">
        <a href="/" class="flex items-center gap-2 text-zinc-900 font-medium hover:text-purple-600 transition-colors">
          <ArrowLeft size={18} />
          <span>Volver</span>
        </a>
        <div class="flex items-center gap-2 text-zinc-900">
            <img src="/Parse_Oprimized.svg" alt="Logo" class="w-6 h-6" />
            <span class="font-['Sixtyfour'] text-xs mt-1">arse</span>
        </div>
      </div>
    </nav>

    <main class="relative z-10 pt-32 pb-24 px-6">
      <div class="max-w-4xl mx-auto">
        <!-- Header -->
        <header class="mb-24 text-center">
          <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-purple-50 border border-purple-100 text-purple-600 text-[10px] font-bold uppercase tracking-wider mb-6">
            Beta Access
          </div>
          <h1 class="text-4xl md:text-7xl font-black text-zinc-900 mb-8 font-['Sixtyfour'] tracking-tighter leading-tight">
            Guía de <br class="md:hidden" /> <span class="text-purple-600">Testers</span>
          </h1>
          <p class="text-xl text-zinc-500 max-w-2xl mx-auto leading-relaxed">
            Tu ayuda es fundamental para perfeccionar la experiencia de lectura. Sigue estos tres pasos para comenzar.
          </p>
        </header>

        <!-- Steps Container (Single Column Focus) -->
        <div class="max-w-3xl mx-auto space-y-24">
          {steps.map((step) => (
            <div class="relative group">
              {/* Step number background */}
              <div class="absolute -top-10 -left-6 md:-left-16 text-[120px] md:text-[180px] font-black text-zinc-100/40 leading-none select-none z-0 font-['Sixtyfour'] group-hover:text-purple-100/50 transition-colors duration-500">
                {step.id}
              </div>

              <div class="relative z-10 bg-white/40 backdrop-blur-sm border border-zinc-100 p-8 md:p-12 rounded-[2.5rem] shadow-sm hover:shadow-xl hover:border-purple-100 transition-all duration-500">
                <div class="flex flex-col md:flex-row md:items-start gap-8">
                    <div class={`shrink-0 w-16 h-16 ${step.color} rounded-2xl flex items-center justify-center shadow-sm`}>
                      <step.icon size={32} />
                    </div>
                    
                    <div class="flex-1">
                        <h2 class="text-3xl md:text-4xl font-bold text-zinc-900 mb-6 font-['Sixtyfour'] tracking-tight">
                        {step.title}
                        </h2>
                        
                        <p class="text-zinc-500 text-lg md:text-xl mb-8 leading-relaxed">
                        {step.description}
                        </p>

                        <ul class="space-y-3 mb-10">
                            {step.details.map(detail => (
                                <li class="flex items-center gap-3 text-zinc-400 text-sm md:text-base">
                                    <div class="w-1.5 h-1.5 rounded-full bg-purple-400"></div>
                                    {detail}
                                </li>
                            ))}
                        </ul>

                        <a 
                        href={step.link} 
                        target="_blank"
                        class="inline-flex items-center gap-3 px-8 py-4 bg-zinc-900 text-white font-bold rounded-2xl hover:bg-black transition-all hover:gap-5 group/btn shadow-lg shadow-zinc-200"
                        >
                        {step.buttonText}
                        <ChevronRight size={20} class="text-zinc-500 group-hover/btn:text-white transition-colors" />
                        </a>
                    </div>
                </div>
              </div>
            </div>
          ))}
        </div>

        <!-- Additional Info -->
        <section class="mt-40 p-10 md:p-16 bg-zinc-900 rounded-[3.5rem] text-white relative overflow-hidden shadow-2xl">
            <div class="absolute -top-10 -right-10 text-zinc-800/30">
                <ShieldCheck size={240} strokeWidth={1} />
            </div>
            
            <div class="relative z-10">
                <h3 class="text-3xl font-bold mb-10 font-['Sixtyfour']">Cosas importantes</h3>
                <div class="grid md:grid-cols-2 gap-12 text-zinc-400 text-base md:text-lg">
                    <div class="space-y-6">
                        <p><strong class="text-white block mb-2">Versiones Beta</strong> Pueden contener errores menores. Te recomendamos tener respaldos de tus libros.</p>
                        <p><strong class="text-white block mb-2">Actualizaciones</strong> Recibirás mejoras y parches con más frecuencia que la versión pública.</p>
                    </div>
                    <div class="space-y-6">
                        <p><strong class="text-white block mb-2">Privacidad</strong> Nuestra <a href="/privacy" class="text-purple-400 hover:underline">Política de Privacidad</a> se aplica igual: tus datos no salen de tu dispositivo.</p>
                        <p><strong class="text-white block mb-2">Feedback</strong> No dudes en reportar cualquier detalle por pequeño que sea. ¡Todo suma!</p>
                    </div>
                </div>
            </div>
        </section>
      </div>
    </main>

    <footer class="py-16 border-t border-zinc-100 bg-zinc-50/50">
        <div class="max-w-5xl mx-auto px-6 text-center">
            <p class="text-xs text-zinc-400 font-bold tracking-[0.2em] uppercase mb-4">&copy; 2026 Parse App</p>
            <p class="text-sm text-zinc-300 font-['Sixtyfour']">La privacidad es un derecho.</p>
        </div>
    </footer>
  </body>
</html>
